<%= form_for(@get_started_pdf) do |f| %>
  <% if @get_started_pdf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@get_started_pdf.errors.count, "error") %> prohibited this get_started_pdf from being saved:</h2>

      <ul>
      <% @get_started_pdf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
